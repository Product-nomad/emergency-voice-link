name = "emergency-voice-link"
compatibility_date = "2024-01-01"

[build]
command = "npm install && npm run build"

[site]
bucket = "./dist"
pages_build_output_dir = "dist"

# Remove service worker format since we're using Pages
# [build.upload]
# format = "service-worker"

[[routes]]
pattern = "/api/*"
script = "api"